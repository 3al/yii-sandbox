<?php/*** Можно действия определять с помощью класса действия.* Это более гибкий подход, позволяет использовать действия повторно.*/class AddAction extends CAction{        //свойства, которые могут быть проинициализированы вызвавшим контроллером    public $property1 = null;    public $property2 = null;        /**    * Действиям можно передавать именованные параметры.    * Если в $_GET есть параметры с ключами var1 и var2,    * то они автоматически попадут в аргументы метода run().    * Если для именованного параметра нет умолчания, а в $_GET он отсутствует,    * будет выброшено исключение  CHttpException (с кодом ошибки 400)    */    public function run(array $var1=array('val1'), $var2='val2'){                //меняем макет для данного действия        $this->getController()->layout = 'funny';                        /* echo "<pre>";        var_dump($this->property1);         var_dump($var1);         var_dump($var2);         echo "</pre>";        exit; */                //логика действия...                //рендерим представление для данного действия        $this->getController()->render('add');                //рендерим представление без макета        //$this->getController()->renderPartial('add');    }    }